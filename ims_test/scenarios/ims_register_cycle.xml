<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!-- 循环注册/注销压力测试场景
     每个 call 执行一次：注册 → 保持 → 注销 → 等待 → 结束
     通过 -m (总 call 数) 控制轮数：-m = limit × rounds
     不设 -m 则无限循环（SIPp 不断创建新 call）

     用法示例:
       sipp <target> -sf ims_register_cycle.xml -inf uac_users.csv \
            -i <local_ip> -p <port> -t un \
            -r <cps> -l <max_concurrent> \
            -key reg_hold_time 5000 \
            -key dereg_pause 1000 \
            -m 10000
     参数:
       reg_hold_time  — 注册后保持时长 (毫秒), 默认 5000
       dereg_pause    — 注销后等待时长 (毫秒), 默认 1000
       -m N           — 总 call 数, 即 limit × rounds（不设则无限）
-->
<scenario name="IMS Register Cycle - Stress Test">
  <Global variables="reg_hold_time,dereg_pause"/>

  <nop>
    <action>
      <assignstr assign_to="reg_hold_time" value="[reg_hold_time]"/>
      <todouble assign_to="reg_hold_time" variable="reg_hold_time"/>
      <assignstr assign_to="dereg_pause" value="[dereg_pause]"/>
      <todouble assign_to="dereg_pause" variable="dereg_pause"/>
    </action>
  </nop>

  <!-- 发送 REGISTER (注册) -->
  <send retrans="500">
    <![CDATA[
      REGISTER sip:[field1] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      From: <sip:[field0]@[field1]>;tag=[call_number]
      To: <sip:[field0]@[field1]>
      Call-ID: [call_id]
      CSeq: [cseq] REGISTER
      Max-Forwards: 70
      Contact: <sip:[field0]@[local_ip]:[local_port];transport=UDP>
      Authorization: Digest username="[field0]@[field1]",realm="[field1]",uri="sip:[field1]",nonce="",response=""
      User-Agent: SIPp IMS Client
      Expires: 3600
      Content-Length: 0
    ]]>
  </send>

  <recv response="100" optional="true">
  </recv>

  <recv response="200" timeout="10000">
  </recv>

  <!-- 保持注册状态一段时间 -->
  <pause variable="reg_hold_time" milliseconds="5000"/>

  <!-- ===== Unregister ===== -->

  <!-- 发送 REGISTER (Unregister, Expires: 0) -->
  <send retrans="500">
    <![CDATA[
      REGISTER sip:[field1] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      From: <sip:[field0]@[field1]>;tag=[call_number]
      To: <sip:[field0]@[field1]>
      Call-ID: [call_id]
      CSeq: [cseq] REGISTER
      Max-Forwards: 70
      Contact: <sip:[field0]@[local_ip]:[local_port];transport=UDP>
      Authorization: Digest username="[field0]@[field1]",realm="[field1]",uri="sip:[field1]",nonce="",response=""
      User-Agent: SIPp IMS Client
      Expires: 0
      Content-Length: 0
    ]]>
  </send>

  <recv response="100" optional="true">
  </recv>

  <recv response="200" timeout="10000">
  </recv>

  <!-- 注销后等待，call 结束。SIPp 按 -m 控制是否创建新 call -->
  <pause variable="dereg_pause" milliseconds="1000"/>

  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000"/>

</scenario>
