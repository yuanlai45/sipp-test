<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!-- UAS 组合主场景: 注册 → 保持进程等待来电完成
     INVITE 由 -oocsf ims_call_uas_demo.xml 处理
     用法:
       ./sipp <pcscf> -sf scenarios/ims_uas_combined.xml \
                      -oocsf scenarios/ims_call_uas_demo.xml \
                      -inf config/test_single_uas.csv \
                      -i <local_ip> -p 10001 -t u1 -m 1 -nd -->
<scenario name="UAS Register and Hold">

  <!-- 注册 -->
  <send retrans="500">
    <![CDATA[
      REGISTER sip:[field1] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      From: <sip:[field0]@[field1]>;tag=[call_number]
      To: <sip:[field0]@[field1]>
      Call-ID: [call_id]
      CSeq: 1 REGISTER
      Max-Forwards: 70
      Contact: <sip:[field0]@[local_ip]:[local_port];transport=UDP>
      Authorization: Digest username="[field0]@[field1]",realm="[field1]",uri="sip:[field1]",nonce="",response=""
      User-Agent: SIPp IMS Client
      Expires: 3600
      Content-Length: 0
    ]]>
  </send>

  <recv response="100" optional="true">
  </recv>

  <recv response="200" timeout="10000">
  </recv>

  <!-- 注册成功，保持进程存活 60 秒等待来电处理完成
       (INVITE 由 -oocsf 指定的 ims_call_uas_demo.xml 自动处理) -->
  <pause milliseconds="60000"/>

  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>
  <CallLengthRepartition value="100, 500, 1000, 5000, 10000"/>

</scenario>
